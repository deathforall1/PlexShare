<UserControl x:Class="Client.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!--defining the grid definitions -->
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--adding the simple hello message on the dashboard for testing purpose -->
        <TextBlock Text="Hello World!! Welcome to the dashboard view " Grid.Row="0"/>
        <!--adding the local view to this dashboard view in order to show the participant list to the end user -->
        
        <local:ParticipantListView  Grid.Row="1"/>
      
    </Grid>
</UserControl>
